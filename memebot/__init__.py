"""memebot — Solana memecoin signal engine with rule-based execution.

Design rule: the LLM (Claude Code) tunes parameters offline via hypotheses;
live decisions are made only by code in `strategy/` and `execution/`.
"""
