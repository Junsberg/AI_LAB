# 데일리 리뷰 2026-09-24 (08:00 KST) — 첫 실행

데이터 창: 약 45분 (22:17–23:02 UTC). 모든 관찰은 **표본 부족** 상태.

## 1. 수집 상태
| 워크플로 | 실행 | 성공 | 실패 |
|---|---|---|---|
| collect | 2 (수동) | 2 | 0 |
| enrich | 1 (수동) | 1 | 0 |

**주의**: 스케줄(10분·1시간) 실행이 아직 한 건도 안 돌았다. main 머지 후 ~50분 경과. GitHub는 신규 cron 등록 후 첫 실행이 수십 분~1시간 지연되는 일이 흔함. 다음 리뷰까지 스케줄 실행이 0이면 워크플로 파일 재등록(빈 변경 커밋 아님, 파일 수정)으로 대응.

## 2. 데이터 품질
| 항목 | 값 |
|---|---|
| tokens | 87 (전부 24h 내) |
| deployers | 71, 자금 추적 완료 25 (36%) — 나머지는 enrich 1회만 돌아 미처리 |
| wallet_edges | 54 |
| clusters | 32, 다중 토큰 클러스터 8 |
| token_outcomes | 0 (24h 경과 토큰 없음, 정상) |
| risk 스냅샷 | 0 — 풀 계정 필터 수정 후 초기화, 재수집 대기 |

## 3. 패턴 관찰 (표본 부족)
1. **연속 발행 배포자**: `6ATnpb…` 가 31분 동안 같은 심볼 `kms` 토큰 5개 마이그레이션. 전형적 스팸/러그 팜 후보. 결과 평가 후 이 배포자 클러스터 점수가 어떻게 되는지 추적.
2. **자금줄 공유 클러스터**: `310e4d57…` 클러스터 = 배포자 3명 · 토큰 4개. 서로 다른 지갑이 같은 자금원에서 나온 사례 — 계보 시그널이 잡으려던 바로 그 구조. 첫 시간에 잡혔다는 건 흔하다는 뜻.
3. **동일 심볼 재발행**: `$HIPER` ×2, `kms` ×5 — 첫 시도 실패 후 재발행하는 패턴. 심볼 재사용 자체를 배포자 태그로 기록할 가치 있음.

## 4. 제안 (우선순위)
1. **스케줄 실행 확인** — 근거: 스케줄이 안 돌면 데이터가 안 쌓인다. 다음 리뷰 전 확인 필수.
2. **`same_symbol_reissue` 태그 추가** — 근거: 관찰 3. 배포자가 같은 심볼을 24h 내 재발행하면 wallets.tags 에 기록 → 계보 점수의 보조 피처.
3. **enrich 추적 한도 상향 (40→80/시간)** — 근거: 배포자 유입 ~70/시간이면 40으로는 적체. Helius 무료 크레딧 기준 홉당 ~4 RPC 콜이라 80도 여유.

## 5. 다음 리뷰 확인 항목
- 스케줄 실행 수 > 0
- risk 스냅샷 top10_pct 중앙값이 10~40% 구간인지 (풀 필터 검증)
- 첫 token_outcomes 등장 (24h 경과분)
