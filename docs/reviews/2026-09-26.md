# 데일리 리뷰 2026-09-26 (08:00 KST)

데이터 창: 24h (2026-09-24 23:08Z → 09-25 23:08Z). 확신도 A=확실 / B=반복 관찰 n≥30 / C=단일·n<30.

## 1. 수집 상태 (24h)
| 워크플로 | 실행 | 실패 | 비고 |
|---|---|---|---|
| collect | 154 (pg_cron 144, GitHub cron 6, push 4) | 0 | 매 10분 정상 |
| enrich | 33 (pg_cron 24, cron 5, push 4) | 0 | |
| outcomes | 56 (pg_cron 47, cron 5, push 2) + 진행 중 2 | 0 | 회당 ~100건 평가(20분 예산) |
| stats | 31 (pg_cron 24, cron 5, push 2) | 0 | |
| ci | 6 | 1 | 내 커밋의 미래 시각 상수 버그 → Postgres 테스트가 잡아 수정(5ca9134) |

프로덕션 워크플로 실패 0. pg_cron 디스패치가 주 경로, GitHub cron은 백업으로 5~6회 — A.

## 2. 오늘 고친 것 (6h 점검 2회)
| 시각(UTC) | 문제 | 수정 | 확신 |
|---|---|---|---|
| 11:00 | outcomes 적체 739건 — GT 429를 4번 맞으면 중단, 회당 9건 평가 | 429는 Retry-After 대기 후 같은 요청 재시도, 20분 시간 예산, pools/multi 배치. 회당 9 → 99건 | A |
| 17:00 | **health `ok`인데 값이 오염**: 고점 배수 최대 56억 배, 10x 11%, price_collapse 249건 | 규칙 v2(거래량 0 캔들 제외, 기준가·고점 모두 캔들 몸통, 1000배 초과는 no_data), 기존 결과 1회 재평가, 건강검진 `peak_multiple_sane` 추가 | A |

재평가 진행률: >100배 36 → 18건(1.1%, 임계 2% 미만으로 warn 해제). 잔여 v1 행 ~400건(최대값 133억 배 행 포함)은 2~3시간 내 소진 예정.

## 3. 데이터 품질
| 항목 | 값 | 전일 대비 |
|---|---|---|
| 토큰 | 2,713 (24h +1,092) | 전일 24h 유입 1,499 → 1,092 (시간당 8~70, 12:00Z 저점 8) |
| 배포자 / 추적 완료 | 1,962 / 1,431 (73%) | 71% → 73% |
| 자금원 wallet / unknown / hub / cex | 1,361 / **267 (13.6%)** / 251 / 68 | unknown 15% → 13.6% |
| 결과 평가 | **1,619** (러그 267 = 16.5%, 10x 118 = 7.3%) | 25 → 1,619 |
| 집중도 top10 중앙값 / p90 | 9.1% / 63% | 8.4% / 52% — p90 상승은 n 증가 효과로 추정(C) |
| 불변식 | 자금 시각 역전 0, 구조 계정 미표시 0, 최대 클러스터 32 지갑 | 정상 |

## 4. 패턴 관찰
1. **러그 267건 전부 `price_collapse`, `lp_pull` 0건 (A, 설계 한계)**. 유동성 고점을 "이전 저장값 vs 현재"로만 잡아서 첫 평가 때는 고점=현재 → lp_pull 조건이 성립 불가. 72h 재평가에서만 가능. 러그율 16.5%는 하한값.
2. **고점 배수 중앙값 1.00 — 모든 버킷·모든 판정에서 (B, n=1,619)**. 기준가가 첫 1시간 몸통 최대이므로, 중앙 토큰은 졸업 후 첫 1시간 이후 한 번도 그 가격을 넘지 못함. 비러그 p90 4.0x. 시사점: 졸업 직후 무차별 진입은 중앙값 기준 손실 — 시그널 선택도가 전부. 동시에 첫 1시간 내 급등(가장 중요한 구간)이 배수에서 사라지는 규칙 v2의 대가.
3. **클러스터 크기별 러그율: single 19.7% (n=803) > 2-3 15.4% (n=292) > 4+ 12.2% (n=524)** (B). 다토큰 클러스터가 오히려 덜 러그. 4+ 버킷에 런치패드 서비스 지갑 클러스터가 섞였을 가능성(관찰 5)이 있어 해석 보류.
4. **첫 "빌더" 후보 클러스터**: `970418…`(평가 11, 러그 0, 10x 4, 배포자 14), `62f6aa…`(11 / 0 / 5 / 14) 점수 0.93 (C). 10x 카운트에 v1 잔여 행이 섞여 있을 수 있어 재평가 완료 후 재확인.
5. **서비스 지갑 후보**: `CdMsg91p…` 16토큰 15심볼(창조자 보유 24%), `D4fvEyoq…` 13/13(7%), `E4mA9Uba…` 13/12(31%) (C). 어제의 `BAGSB9…`(보유 0%)와 달리 보유율이 있어 사람일 가능성. 분류 규칙은 보류.
6. **unknown 자금원 첫 거래 분포**: `System transfer` 241, `syncNative` 92, `closeAccount` 79, `createIdempotent` 80. transfer가 있는데 unknown = 지갑이 *보내는* 쪽(수수료·프로그램 납부)이거나 목적지가 타 계정. `syncNative/closeAccount` = WSOL 랩·언랩 → 스왑 수익으로 SOL을 얻은 지갑 (B). 계보 관점에선 "자금원 = DEX"라 추적 불가가 맞고, unknown이 아닌 `swap`으로 표기하는 게 정확.

## 5. 제안 (우선순위)
1. **유동성 고점 바닥값 저장** — 근거: 관찰 1. collect 시 GT `reserve_in_usd`(이미 응답에 있음)를 `meta.reserve_usd_at_seen`에 저장, 평가 시 유동성 고점 = max(저장값, 현재). 코드 10줄, 수집 즉시 적용, lp_pull이 첫 평가에서도 동작. (A)
2. **첫 24h는 5분 캔들로 평가** — 근거: 관찰 2. GT는 `minute` 캔들 aggregate=5, limit 1000(≈83h) 지원. 기준가 = 졸업 +15분 몸통, 고점 = 5분 몸통 최대 → 첫 1시간 급등 복원. 호출 수 동일(토큰당 1콜). 재평가 트리거는 `RULES_CHANGED_AT` 갱신으로 재사용. (B)
3. **unknown → `swap` 세분화** — 근거: 관찰 6. 첫 5개 거래에 `syncNative`/`closeAccount`(WSOL)가 있고 System transfer 입금이 없으면 `source_type='swap'`. 계보 결합엔 영향 없음(둘 다 결합 안 함), 통계·건강검진 `trace_quality`의 분모만 정확해짐. (B)

## 6. 다음 리뷰 확인 항목
- v1 잔여 행 0 → `outcomes_summary.max_peak` ≤ 1000, `peak_multiple_sane` ok 유지
- 제안 1 적용 후 `lp_pull` 첫 등장
- 빌더 후보 클러스터 2개의 10x 카운트 재확인
- 72h 재평가 첫 물결(09-26 오후) 후 러그율 변화
